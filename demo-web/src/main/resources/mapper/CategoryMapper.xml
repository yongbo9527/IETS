<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.company.finance.infrastructure.persistence.mapper.CategoryMapper">


    <select id="selectDataCategory" resultType="org.company.finance.application.vo.category.DataCategoryVO">
        SELECT
            record.category_id,
            category.category_name,
            category.parent_id
        FROM
            daily_expense_record record
                LEFT JOIN base_tally_category category ON record.category_id = category.id
        WHERE
            record.del_flag = 0
          AND record.expense_date BETWEEN #{startDate}
            AND #{endDate}
        ORDER BY
            record.category_id
    </select>
</mapper>
